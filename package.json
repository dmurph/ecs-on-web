{
  "name": "ecs-experiment",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "asbuild": "asc assembly/index.ts -o public/benchmark_wasm_ecs.wasm -O --noAssert",
    "dev": "npm run asbuild && vite",
    "build": "npm run asbuild && tsc && vite build",
    "preview": "vite preview",
    "benchmark": "npm run asbuild && npx tsx scripts/run_benchmarks.ts"
  },
  "devDependencies": {
    "assemblyscript": "^0.28.18",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "bitecs": "^0.4.0"
  }
}
